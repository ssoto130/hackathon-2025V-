# TODO

- TODO: place to see the credits and libraries used, could just be a learn page and we hyperlink to it
- TODO: make the learn page which will take markdown
- TODO: a practice page where someone can easily test out code, code blocks can have a button that takes you to a code page with that code loaded so you can test it but for time purposes could have it copy to clipboard and send you to a online java editor

src/lib/frames/ContentRenderer

- TODO: add a interactable code thing so you can see how a piece of code runs
- TODO: if link is a youtube video, display the embed

src/routes/problems/[questionSetId]:

- TODO: save file content to cookies and load it too
- TODO: pop up if it failed to load a question
- TODO: fix the bug when fully expanding a pane

src/lib/code_blocks/BaseCodeEditor

- TODO: a mode where a user can add files and such


FIX THE ROUTES IF THEY ACCEPT PARAMS TO CHECK IF THEY EXIST

ui bugs that happen when the window is too small

add font choices for the editor